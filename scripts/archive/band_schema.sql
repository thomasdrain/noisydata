create table metalarchives.Band (
	BandID bigint,
    BandName varchar(255),
    BandLink varchar(255),
    BandStatus varchar(255),
    Country varchar(255),
    Genre varchar(255),
    Scraped datetime
);